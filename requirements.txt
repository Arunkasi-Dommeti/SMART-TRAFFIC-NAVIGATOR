# Smart Traffic Navigator — Backend Dependencies
# Install: pip install -r requirements.txt
# Python : 3.9+

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database ORM + PostgreSQL driver
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Data validation
pydantic==2.7.1

# Environment variable support
python-dotenv==1.0.1

# Firebase Admin SDK — connects to same Firebase RTDB used by all modules
# (ambulance-module.ino, EMT_interface.html, Hospital-dash-board.html)
# Setup: Firebase Console → Project Settings → Service Accounts → Generate key
firebase-admin==6.4.0

# HTTP client — Google Maps ETA per junction (Phase 4 roadmap item)
# Not yet imported in main.py; listed so Phase 4 dev has no extra setup step
httpx==0.27.0