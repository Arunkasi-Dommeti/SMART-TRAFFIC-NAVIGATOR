# Smart Traffic Navigator — Backend Dependencies
# Install: pip install -r requirements.txt
# Python : 3.9+

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database ORM + PostgreSQL driver
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Data validation
pydantic==2.7.1

# Environment variable support
python-dotenv==1.0.1

# HTTP client — Gemini AI proxy in main.py (/api/v1/ai/first-aid endpoint)
httpx==0.27.0

# NOTE: firebase-admin removed — main.py uses PostgreSQL via SQLAlchemy, not Firebase Admin SDK.
# The Firebase RTDB used by ESP32 firmware and HTML dashboards is accessed directly
# via the Firebase JS SDK (HTML files) and Firebase_ESP_Client (Arduino .ino files).
# Re-add firebase-admin only if you implement server-side Firebase Admin operations.